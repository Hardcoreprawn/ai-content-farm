# Flexible common dependencies for fast builds and easy maintenance
# Using compatible releases (~=) for automatic security patches + Dependabot updates
# Updated to latest stable versions to minimize Dependabot PRs
# Last updated: 2025-09-03 for latest stable versions

# Core Web Framework (used in 7/8 containers)
fastapi~=0.116.1
uvicorn[standard]~=0.35.0  
pydantic~=2.11.7
pydantic-settings~=2.10.1

# HTTP & Networking (used in 6+ containers)  
httpx~=0.28.1
python-multipart~=0.0.20

# Azure Core (used in 6+ containers)
azure-storage-blob~=12.26.0
azure-identity~=1.24.0

# Azure Service Bus for Phase 1 Security Implementation
azure-servicebus~=7.12.3

# Testing Framework (used in 8/8 containers)
pytest~=8.2.0
pytest-asyncio~=1.0.0
