# Flexible common dependencies for fast builds and easy maintenance
# Using compatible releases (>=) for automatic security patches
# Last updated: 2025-08-22 for flexibility optimization

# Core Web Framework (used in 7/8 containers)
# Using compatible releases (~=) for automatic patch updates + Dependabot minor/major updates
fastapi~=0.116.0
uvicorn[standard]~=0.24.0  
pydantic~=2.5.0

# HTTP & Networking (used in 6+ containers)  
httpx~=0.25.0
python-multipart~=0.0.15

# Azure Core (used in 6+ containers)
azure-storage-blob~=12.19.0

# Testing Framework (used in 8/8 containers)
pytest~=8.2.0
pytest-asyncio~=1.0.0
