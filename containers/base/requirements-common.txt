# Optimized common dependencies for fast builds
# Only the MOST common dependencies (6+ containers) to maximize cache hits
# Last updated: 2025-08-22 for build speed optimization

# Core Web Framework (used in 7/8 containers)
fastapi==0.116.1
uvicorn[standard]==0.35.0
pydantic==2.11.7

# HTTP & Networking (used in 6+ containers)
httpx==0.28.1
python-multipart==0.0.20

# Azure Core (used in 6+ containers)
azure-storage-blob==12.26.0

# Testing Framework (used in 8/8 containers)
pytest==8.4.1
pytest-asyncio==1.1.0
