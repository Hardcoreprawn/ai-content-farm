# UNIQUE dependencies for content-generator only
# All common deps come from base layers

# AI Services (unique to content-generator)
openai==1.59.0
anthropic==0.42.0

# Azure Service Bus (unique to content-generator)  
azure-servicebus==7.13.1

# That's it! FastAPI, uvicorn, pydantic, Azure identity, etc. 
# all come from the base layers
