[pytest]
# Site Generator specific pytest configuration
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

# Register marks to avoid warnings
markers =
    unit: Unit tests
    integration: Integration tests
    security: Security tests
