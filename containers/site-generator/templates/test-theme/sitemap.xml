<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    <url>
        <loc>{{ base_url }}</loc>
        <lastmod>{{ last_updated.strftime('%Y-%m-%d') if last_updated else current_date }}</lastmod>
        <changefreq>daily</changefreq>
        <priority>1.0</priority>
    </url>
    {% for article in articles %}
    <url>
        <loc>{{ base_url }}/{{ article.slug }}.html</loc>
        <lastmod>{{ article.generated_at.strftime('%Y-%m-%d') if article.generated_at else current_date }}</lastmod>
        <changefreq>weekly</changefreq>
        <priority>0.8</priority>
    </url>
    {% endfor %}
</urlset>
