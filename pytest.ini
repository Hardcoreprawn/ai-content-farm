[pytest]
# Main pytest configuration moved to config/pytest.ini for organization
# This file exists only to point pytest to the correct configuration location
addopts = -c config/pytest.ini

# Asyncio settings need to be in the main pytest.ini to avoid deprecation warnings
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
