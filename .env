# AI Content Farm - Local Development Environment Variables
# Copy to .env and configure as needed

# Azure Key Vault Configuration (for secure secret management)
# Connect to your Azure Key Vault in the cloud for credential retrieval
AZURE_KEY_VAULT_URL=https://your-keyvault.vault.azure.net/
AZURE_CLIENT_ID=your-service-principal-client-id
AZURE_CLIENT_SECRET=your-service-principal-client-secret
AZURE_TENANT_ID=your-azure-tenant-id

# Reddit API Configuration (fallback if not in Key Vault)
# For production or heavy usage, get credentials from https://www.reddit.com/prefs/apps
# These serve as fallback if Key Vault is not configured
REDDIT_CLIENT_ID=your-reddit-client-id
REDDIT_CLIENT_SECRET=your-reddit-client-secret
REDDIT_USER_AGENT=ai-content-farm-local/1.0

# OpenAI API Configuration (for content enrichment)
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key

# Local Development Settings
ENVIRONMENT=development
DEBUG=true

# Optional: Override default settings
# MAX_REQUESTS_PER_MINUTE=60
# DEFAULT_POSTS_PER_SUBREDDIT=10
# SIMILARITY_THRESHOLD=0.8

# Local Development Notes:
# - Azurite automatically provides Azure Storage emulation
# - Key Vault integration allows secure credential management
# - Reddit content collection works without credentials (with rate limits)
# - All services should start and be testable without any credentials
